targets:
  $default:
    builders:
      build_web_compilers|entrypoint:
        release_options:
          dart2js_args:
            - --fast-startup
            - --minify
            - --trust-type-annotations
            - --trust-primitives
