name: hacker_news_pwa
environment:
  sdk: '>=2.0.0-dev.20.0 <2.0.0'

dependencies:
  angular: ^5.0.0-alpha
  angular_router: ^2.0.0-alpha
  http: ^0.11.3+13
  pwa: ^0.1.7

# DO NOT REMOVE. We don't publish this package, it is just for testing.
dependency_overrides:
  angular:
    path: ../../angular
  angular_ast:
    path: ../../angular_ast
  angular_compiler:
    path: ../../angular_compiler
  angular_router:
    path: ../../angular_router
  # Removable after https://github.com/isoos/pwa/pull/18.
  args: '^1.0.0'
