targets:
  $default:
    builders:
      build_web_compilers|entrypoint:
        generate_for:
        - web/**.dart
        options:
          dart2js_args:
          - --preview-dart-2
          - --dump-info
          - --fast-startup
          - --minify
          - --trust-type-annotations
          - --trust-primitives
