name: examples.hello_world_no_reflector

dependencies:
  angular: ^5.0.0-alpha

dev_dependencies:
  build_runner: ^0.7.9
  build_test: ^0.10.0
  build_web_compilers: ^0.3.0

# DO NOT REMOVE. We don't publish this package, it is just for testing.
dependency_overrides:
  angular:
    path: ../../angular
  angular_ast:
    path: ../../angular_ast
  angular_compiler:
    path: ../../angular_compiler
