targets:
  $default:
    builders:
      build_web_compilers|entrypoint:
        generate_for:
        - web/**.dart
        - test/**.dart
        options:
          compiler: dart2js
          dart2js_args:
          - --minify
          - --trust-type-annotations
          - --trust-primitives
