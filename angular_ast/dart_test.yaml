# TODO(https://github.com/dart-lang/test/pull/777):
# Add this to a common dart_test.yaml configuration.
presets:
  travis:
    tags:
      fails-on-travis:
        skip: 'This test currently fails on travis. See code for TODOs.'
    reporter: expanded

platforms:
  - vm
